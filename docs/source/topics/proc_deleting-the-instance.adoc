[id="deleting-the-instance_{context}"]
= Deleting the instance

The [command]`{bin} delete` command deletes an existing {prod} instance.

.Procedure

* Delete the {prod} instance:
+
include::snip_crc-delete.adoc[]
